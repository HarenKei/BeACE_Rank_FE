import styled from "styled-components";
import { useEffect, useState } from "react";

const Pagination = ({maxPage, pageLimit, curPage} : {maxPage: number, pageLimit: number, curPage: number}) => {

  return(
    <>
    </>
  );
};

export default Pagination;